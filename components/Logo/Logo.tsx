export function Logo({classStyle = ''}) {
    const classname = `scroll-m-20 pb-2 text-3xl font-bold tracking-tight first:mt-0 ${classStyle}`
    return (
      <h2 className={classname}>
        Wraply
      </h2>
    )
  }
  